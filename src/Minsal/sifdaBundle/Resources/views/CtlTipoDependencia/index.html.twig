{#{% extends '::base.html.twig' %}#}

{% extends 'MinsalsifdaBundle::layoutResponsable.html.twig' %}


{% block contenido_in -%}
    <div class="page-header">
        <center><h3><center>Listado de tipos de dependencia</center></h3></center>
    </div>
    

    <table class="table table-bordered table-hover table-condensed">
        <thead>
            <tr>
                <th>Correlativo</th>
                <th>Nombre</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('sifda_ctltipodependencia_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.nombre }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('sifda_ctltipodependencia_show', { 'id': entity.id }) }}">Mostrar</a>
                    </li>
                    <li>
                        <a href="{{ path('sifda_ctltipodependencia_edit', { 'id': entity.id }) }}">Editar</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        {#<ul>
        <li>
            <a href="{{ path('sifda_ctltipodependencia_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>#}
    {% endblock %}
