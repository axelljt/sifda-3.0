{% extends 'MinsalsifdaBundle::layoutTecnico.html.twig' %}

{% block contenido_in -%}
    <div class="page-header">
            <center><h3>Mostrar recurso asignado a una orden de trabajo</h3></center>
    </div>
    
    <table id="tabla" class="table table-bordered table-hover table-condensed">
        <tbody>
            {#<tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>#}
            <tr>
                <th>Cantidad</th>
                <td>{{ entity.cantidad }}</td>
            </tr>
            <tr>
                <th>Costo total ($)</th>
                <td>{{ entity.costoTotal }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ entity.idTipoRecursoDependencia.idTipoRecurso.nombre }}</td>
            </tr>
        </tbody>
    </table>
                    <div class="row-fluid">    
                        <div class="span12">
                        <br>
                        <center>
                            <table id="botones">
                            <tr> 
                               {#<td>{{form_row(form.submit,{'attr':{'class':'btn btn-primary'}})}}</td>#}
                               <a href="{{ path('sifdarecursoservicio', { 'idInf': entity.idInformeOrdenTrabajo.id }) }}">
                                </td>
                            </tr>    
                            </table>
                        </center>
                        <br>
                        </div>
                    </div>
                                
                                <center>
                    <table id="botones">
                    <tr> 
                       
                        <td>
                            <a href="{{ path('sifdainformeordentrabajo', { 'idOrd': entity.idInformeOrdenTrabajo.idOrdenTrabajo.id })}}" class="btn">Regresar</a>
                        </td>
                    </tr>    
                    </table>
                </center>
        {#<ul class="record_actions">
    <li>
        <a href="{{ path('sifdarecursoservicio', { 'idInf': entity.idInformeOrdenTrabajo.id }) }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('sifdarecursoservicio_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>#}
{% endblock %}
