{% extends '::base.html.twig' %}

{% block body -%}
    <h1>SidplaSubactividad</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ entity.descripcion }}</td>
            </tr>
            <tr>
                <th>Codigo</th>
                <td>{{ entity.codigo }}</td>
            </tr>
            <tr>
                <th>Activo</th>
                <td>{{ entity.activo }}</td>
            </tr>
            <tr>
                <th>Metaanual</th>
                <td>{{ entity.metaAnual }}</td>
            </tr>
            <tr>
                <th>Descripcionmetaanual</th>
                <td>{{ entity.descripcionMetaAnual }}</td>
            </tr>
            <tr>
                <th>Indicador</th>
                <td>{{ entity.indicador }}</td>
            </tr>
            <tr>
                <th>Medioverificacion</th>
                <td>{{ entity.medioVerificacion }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('sidplasubactividad') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('sidplasubactividad_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
