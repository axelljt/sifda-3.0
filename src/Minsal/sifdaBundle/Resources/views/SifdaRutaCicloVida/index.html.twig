{% extends '::base.html.twig' %}

{% block body -%}
    <h1>SifdaRutaCicloVida list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Descripcion</th>
                <th>Ref1</th>
                <th>Jerarquia</th>
                <th>Ignorarsig</th>
                <th>Peso</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('sifda_rutaciclovida_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.descripcion }}</td>
                <td>{{ entity.ref1 }}</td>
                <td>{{ entity.jerarquia }}</td>
                <td>{{ entity.ignorarSig }}</td>
                <td>{{ entity.peso }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('sifda_rutaciclovida_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('sifda_rutaciclovida_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('sifda_rutaciclovida_new', { 'id': entity.id }) }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
