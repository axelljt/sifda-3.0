{#{% extends '::base.html.twig' %}#}
{% extends 'MinsalsifdaBundle::layoutSolicitante.html.twig' %}
{% block body -%}
    <h1>CtlDependencia</h1>

    <table class="table table-bordered table-hover table-condensed">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ entity.nombre }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('sifda_ctldependencia') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('sifda_ctldependencia_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
